---
id: consistent-naming
title: Follow established naming conventions
applies_to: all
severity: advisory
---

## Rule

New identifiers (variables, functions, files, plan slugs) must follow the naming convention already established in the file or directory. Do not introduce a new convention in a single file. When no convention exists, prefer kebab-case for files and camelCase for JavaScript/TypeScript identifiers.
