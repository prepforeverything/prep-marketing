name: HSK ad-ops hằng ngày

# HSK (engine nv, 3 TK Meta) — nhóm "Digital HSK". Engine ad-ops headless trên GitHub Actions (always-on). Cron LUÔN theo UTC → 10:07 VN = 03:07 UTC, 14:07 VN = 07:07 UTC.
# Chống gửi trùng BỀN VỮNG: cờ "đã gửi" commit vào repo (automation/products/hsk/state/). Mỗi lần chạy đọc
# cờ mới nhất từ main → đã có cờ ngày mục tiêu thì SKIP. Đảm bảo gửi đúng 1 lần dù cron bị trễ/lặp.
# ⚠️ Lịch chỉ chạy khi file này nằm trên nhánh mặc định (main).
on:
  schedule:
    - cron: "7 3 * * *"   # 10:07 giờ VN
    - cron: "7 7 * * *"   # 14:07 giờ VN
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Chạy thử — KHÔNG gửi Telegram"
        type: boolean
        default: false

concurrency:
  group: hsk-adops      # serialize: lần chạy sau chỉ bắt đầu (và checkout cờ mới) sau khi lần trước xong
  cancel-in-progress: false

permissions:
  contents: write          # để commit cờ "đã gửi" vào repo

jobs:
  run:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Ho_Chi_Minh
      META_ACCESS_TOKEN: ${{ secrets.META_ACCESS_TOKEN }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_HSK_CHAT_ID: ${{ secrets.TELEGRAM_HSK_CHAT_ID }}
      PREP_BI_API_KEY: ${{ secrets.PREP_BI_API_KEY }}
    steps:
      - uses: actions/checkout@v4   # lấy main mới nhất → gồm cờ "đã gửi" của lần chạy trước trong ngày

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cài Chrome (để xuất PDF)
        id: chrome
        uses: browser-actions/setup-chrome@v1

      - name: Chạy ad-ops TOEIC
        env:
          CHROME_BIN: ${{ steps.chrome.outputs.chrome-path }}
          CHROME_EXTRA_ARGS: "--no-sandbox --disable-dev-shm-usage"
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            python3 automation/engine/run_daily.py --product hsk --dry-run
          else
            python3 automation/engine/run_daily.py --product hsk
          fi

      - name: Lưu cờ "đã gửi" vào repo (chống gửi trùng)
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: |
          if [ -n "$(git status --porcelain automation/products/hsk/state)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add automation/products/hsk/state
            git commit -m "chore(adops): HSK đã gửi báo cáo $(date +%F) [skip ci]"
            for i in 1 2 3; do
              git pull --rebase origin main && git push origin HEAD:main && break
              echo "push lần $i trượt (PTE/commit khác vừa vào main) — rebase & thử lại"; sleep 3
            done
          else
            echo "Không gửi lần này (SKIP/alert) — không có cờ mới để commit."
          fi
