{
  "_note": "Team configuration for the marketing kit. Written by /mkt-setup; safe to hand-edit. Hooks, scripts, commands, agents, and skills read these values instead of hardcoding company / locale / market / governance. Delete this file to fall back to neutral defaults (primaryMarket 'VN', publishGate 'warn').",
  "companyName": "PrepEdu",
  "primaryLocale": "vi-VN",
  "primaryMarket": "VN",
  "markets": ["VN"],
  "businessType": "education / test-prep (IELTS & English exam prep)",
  "governance": {
    "publishGate": "warn",
    "_publishGate_values": "off = no claims gate on publish-ready writes | warn = flag but allow (advisory default) | deny = block publish-ready writes with unverified claims (opt-in strict, fail-closed)"
  },
  "publish": {
    "_note": "How approved landing pages go live (read by /mkt-publish + publish-landing.mjs). After the one-time Phase-0 setup, set repo.remote to your real publish repo. Cloudflare Pages serves it at https://<subdomain>/<localeSegment>/<slug>/. localeSegment defaults to the primaryLocale language subtag (vi) when omitted.",
    "provider": "cloudflare-workers-assets",
    "subdomain": "lp.prepedu.com",
    "projectName": "prepedu-landing",
    "localeSegment": "vi",
    "repo": {
      "remote": "https://github.com/namht1st/prepedu-landing.git",
      "productionBranch": "main",
      "localCheckout": ".prepkit/.publish-cache/prepedu-landing"
    }
  }
}
